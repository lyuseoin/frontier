# AGENTS.md — 필터버블 진단 & 알고리즘 디톡스 프로젝트

## 과제
- `docs/plan.md`를 구현해야 함

### 기술
- 프로그래밍 언어: python
- 프레임워크: Streamlit
- 디자인가이드: `docs/design_guide.md`
- AI 알고리즘이 해당 데이터의 성향과 편향도를 계산하는 부분은 GEMINI API를 이용하고, `.env` 파일의 GEMINI_API_KEY 를 활용할 것

## 가장 중요한 원칙
- 요구사항 달성을 위한 최소한의 코드만 작성
- 수정이 필요한 최소한의 범위만 수정. 무분별하게 인접한 파일 수정 금지
- 검증 가능한 목표를 수립하고, 목표가 달성될 떼 까지 반복 실행
- 실패했을 경우, 동일한 오류를 반복하지 않도록 `docs/lessons.md` 에 저장하고, 다음 실행시 확인

### 진행
- 반드시 계획을 작성하고 승인 받은 후 진행할 것
- 가정하지 말 것. 불확실한 내용이 있으면 추측하지 말고 반드시 질문할 것. 질문할 때는 설명과 함께 선택지를 제시할 것
- 작업이 완료되었다고 선언하기 전에 테스트를 통해 의도대로 작성되었는 지 검증할 것

### 제약
- 세션당 30,000 토큰 넘기지 말 것. 한계에 도달하기 전에 내용을 요약하고 세션을 초기화할 것
- AI는 분류, 요약 등 판단에 집중하고, 그 외 작업은 코드를 통해서 처리할 것
- 코드 작성 전 의존성과 호출 구조를 파악할 것. 코드의 구조적 이유를 모르고 수정하면 안됨
- 패턴이 충돌할 경우 검증된 것을 선택할 것
- `docs/do_not_read` 는 절대 살펴보지 말 것
- `docs` 폴더 안의 파일은 절대 수정하지 말 것
  
### 피드백
- 문제가 발생하면 `docs/lessons.md`에 기록할 것
- 동일한 실수가 반복적으로 발생하면 새로운 규칙을 만들어서 `AGENTS.md`에 반영할 것
- 매 단계마다 수행결과, 검증상태, 남은과제를 정리하여 상태를 명확히 유지할 것